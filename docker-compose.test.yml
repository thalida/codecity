# Contributor-only: every test, lint and typecheck service, in containers. Each
# package defines its own; this file only combines them. Run via `just test`.
include:
  - packages/api/docker-compose.test.yml
  - packages/app/docker-compose.test.yml
  - packages/city/docker-compose.test.yml
