{
  "name": "codecity-repo",
  "version": "0.0.0",
  "description": "Repo-root tooling for codecity: prettier and its config, so formatting resolves the same way from anywhere in the tree.",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "license": "AGPL-3.0",
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
