{
  "name": "codecity-repo",
  "version": "0.0.0",
  "description": "Workspace root for codecity: the web app, the packages it is being extracted into, and the prettier config they all share.",
  "private": true,
  "type": "module",
  "workspaces": [
    "app",
    "city",
    "client"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "license": "AGPL-3.0",
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
